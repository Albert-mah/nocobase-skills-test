const t=(ctx.record||{}).{FIELD};if(!t){ctx.render(ctx.React.createElement('span',{style:{color:'#bbb'}},'-'));return;}const diff=(Date.now()-new Date(t).getTime())/1000;let text,warn=false;if(diff<60)text='刚刚';else if(diff<3600)text=Math.floor(diff/60)+'分钟前';else if(diff<86400)text=Math.floor(diff/3600)+'小时前';else{text=Math.floor(diff/86400)+'天前';warn=diff>604800;}ctx.render(ctx.React.createElement('span',{style:{color:warn?'#fa8c16':'#8c8c8c',fontSize:13}},text));
